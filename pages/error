

st.markdown('---')

st.subheader("Cross-Validation Results (5-Fold)")


cv_scores = cross_val_score(full_pipeline, X, y, cv=5)

st.write(f"CV Scores: **{np.round(cv_scores, 4)}**")
st.write(f"Mean CV Accuracy: **{cv_scores.mean():.4f}**")

cv_scores = cross_val_score(full_pipeline, X, y, cv=5)

cv_scores = cross_val_score(rf_pipeline, X, y, cv=5)
print("CV Scores:", cv_scores)
print("Mean CV:", cv_scores.mean())



